[project]
name = "EnhancedEdit"
version = "0.2.9"
description = "Enhanced Editor"
authors = [{ name = "Brian Davis (bb6)" }]
dependencies = [
    "numpy"
]

[tool.lichtfeld]
hot_reload = true
plugin_api = ">=1,<2"
lichtfeld_version = ">=0.5.1"
required_features = []
